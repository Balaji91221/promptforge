// Injected by Vite's `define` (see vite.config.ts).
declare const __PF_API__: string;
// Optional cloud sync base URL (Phase 1+); empty string disables sync.
interface GlobalThis {
  __PF_SYNC__?: string;
}
