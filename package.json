{
  "name": "promptforge",
  "private": true,
  "version": "0.0.0",
  "description": "PromptForge — cross-provider prompt-engineering assistant. Phase 0 validation MVP.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "tsc -b",
    "eval": "tsx evals/runner.ts",
    "e2e": "tsc -b packages/types packages/core packages/adapters && tsx evals/e2e-extension.ts",
    "clean": "rm -rf packages/*/dist packages/*/*.tsbuildinfo apps/*/dist apps/*/.next",
    "dev:ext": "npm run dev --workspace apps/extension-browser"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
